# ============================================
# CONEXIÓN SUPABASE POSTGRESQL
# ============================================

# URL de conexión a Supabase (cambia [YOUR-PASSWORD] por tu contraseña real)
spring.datasource.url=jdbc:postgresql://db.kizsqesjtgbxhaghemme.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=IngecAR@Proyectsis

# IMPORTANTE: Pool reducido para plan free de Supabase
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.connection-timeout=30000

# ============================================
# JPA CONFIGURACIÓN
# ============================================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=America/Lima

# ============================================
# SERVER
# ============================================

server.port=${PORT:8080}
spring.jackson.time-zone=America/Lima

# ============================================
# THYMELEAF
# ============================================

spring.thymeleaf.cache=false